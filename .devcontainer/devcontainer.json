{
  "name": "fine-grained-rbac-auth-model-backend",
  "image": "mcr.microsoft.com/devcontainers/base:bookworm",
  "runArgs": [
    "--privileged",
    "--device=/dev/fuse"
  ],
  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "25"
    },
    "ghcr.io/devcontainers/features/sshd:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [],
      "settings": {
        "java.jdt.ls.java.home": "/usr/local/sdkman/candidates/java/current"
      }
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y podman uidmap slirp4netns fuse-overlayfs && chmod +x gradlew && ./gradlew --version && podman --version"
}
